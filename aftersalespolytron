<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Log Book After Sales — M1</title>
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<style>
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

:root {
  --red: #C0392B;
  --red-dark: #922B21;
  --red-light: #FDF0EF;
  --red-mid: #E8C9C6;
  --text: #1A1A1A;
  --sub: #555;
  --muted: #999;
  --border: #E8E6E1;
  --bg: #F5F3EF;
  --white: #FFFFFE;
  --gray: #EEECEA;
  --ink: #2C2C2C;
}

body {
  font-family: 'Plus Jakarta Sans', sans-serif;
  background: var(--bg);
  color: var(--ink);
  min-height: 100vh;
  padding-bottom: 80px;
}

/*  TOP BAR  */
.topbar {
  background: var(--red);
  padding: 0 16px;
  height: 52px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  position: sticky;
  top: 0;
  z-index: 100;
}
.topbar-title {
  font-size: 15px;
  font-weight: 800;
  color: #fff;
  letter-spacing: -0.3px;
}
.topbar-right {
  display: flex;
  gap: 8px;
}
.topbar-btn {
  background: rgba(255,255,255,0.15);
  border: none;
  border-radius: 8px;
  color: #fff;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 12px;
  font-weight: 600;
  padding: 6px 12px;
  cursor: pointer;
  transition: background .15s;
}
.topbar-btn:hover { background: rgba(255,255,255,0.25); }
.topbar-btn.primary { background: rgba(255,255,255,0.9); color: var(--red); }
.topbar-btn.primary:hover { background: #fff; }

/*  TAB NAV  */
.tabnav {
  background: var(--white);
  border-bottom: 1px solid var(--border);
  display: flex;
  overflow-x: auto;
  scrollbar-width: none;
  position: sticky;
  top: 52px;
  z-index: 99;
}
.tabnav::-webkit-scrollbar { display: none; }
.tab {
  flex: 1;
  min-width: 80px;
  padding: 12px 8px 10px;
  text-align: center;
  cursor: pointer;
  border-bottom: 3px solid transparent;
  transition: all .2s;
  white-space: nowrap;
}
.tab.active { border-bottom-color: var(--red); }
.tab-icon { font-size: 16px; display: block; margin-bottom: 2px; }
.tab-label { font-size: 10px; font-weight: 700; color: var(--muted); text-transform: uppercase; letter-spacing: 0.3px; }
.tab.active .tab-label { color: var(--red); }

/*  VIEWS  */
.view { display: none; }
.view.active { display: block; }

/*  COVER  */
.cover {
  padding: 24px 16px;
  max-width: 560px;
  margin: 0 auto;
}
.cover-card {
  background: var(--white);
  border-radius: 16px;
  overflow: hidden;
  border: 1px solid var(--border);
}
.cover-top {
  background: var(--red);
  padding: 28px 24px 22px;
}
.cover-badge {
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 2px;
  color: rgba(255,255,255,0.6);
  text-transform: uppercase;
  margin-bottom: 8px;
}
.cover-title {
  font-size: 26px;
  font-weight: 800;
  color: #fff;
  margin-bottom: 4px;
  line-height: 1.2;
}
.cover-sub { font-size: 13px; color: rgba(255,255,255,0.7); }
.cover-fields { padding: 20px 24px; display: flex; flex-direction: column; gap: 14px; }
.cover-field label {
  font-size: 11px;
  font-weight: 700;
  color: var(--muted);
  text-transform: uppercase;
  letter-spacing: 0.8px;
  display: block;
  margin-bottom: 5px;
}
.cover-field input, .cover-field select {
  width: 100%;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 15px;
  font-weight: 500;
  color: var(--ink);
  border: none;
  border-bottom: 2px solid var(--border);
  padding: 6px 0;
  background: transparent;
  outline: none;
  transition: border-color .2s;
  appearance: none;
}
.cover-field input:focus, .cover-field select:focus { border-bottom-color: var(--red); }
.cover-field input::placeholder { color: #CCC; font-style: italic; font-weight: 400; }
.cover-field-row { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }

.cover-tip {
  margin: 20px 16px 0;
  background: var(--red-light);
  border-left: 3px solid var(--red);
  border-radius: 8px;
  padding: 12px 14px;
  font-size: 13px;
  color: var(--red-dark);
  line-height: 1.6;
}

/*  ENTRIES  */
.entries-wrap {
  padding: 16px;
  max-width: 560px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.entry-card {
  background: var(--white);
  border-radius: 14px;
  border: 1px solid var(--border);
  overflow: hidden;
}
.entry-header {
  padding: 14px 16px 12px;
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 10px;
  border-bottom: 1px solid var(--border);
  cursor: pointer;
}
.entry-header-left { flex: 1; }
.entry-date-loc { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; margin-bottom: 4px; }
.entry-date {
  font-size: 12px;
  font-weight: 700;
  color: var(--red);
  background: var(--red-light);
  padding: 2px 8px;
  border-radius: 20px;
}
.entry-loc {
  font-size: 13px;
  font-weight: 600;
  color: var(--ink);
}
.entry-preview {
  font-size: 12px;
  color: var(--muted);
  line-height: 1.5;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 260px;
}
.entry-actions { display: flex; gap: 6px; flex-shrink: 0; }
.entry-btn {
  width: 32px; height: 32px;
  border-radius: 8px;
  border: 1.5px solid var(--border);
  background: var(--gray);
  cursor: pointer;
  font-size: 14px;
  display: flex; align-items: center; justify-content: center;
  transition: all .15s;
  color: var(--muted);
}
.entry-btn:hover { border-color: var(--red); color: var(--red); background: var(--red-light); }
.entry-btn.del:hover { border-color: #E74C3C; color: #E74C3C; background: #FDEDED; }

.entry-body { display: none; padding: 16px; }
.entry-body.open { display: block; }

/*  ENTRY SECTIONS  */
.efield { margin-bottom: 16px; }
.efield-label {
  font-size: 11px;
  font-weight: 700;
  color: var(--muted);
  text-transform: uppercase;
  letter-spacing: 0.7px;
  margin-bottom: 6px;
  display: flex;
  align-items: center;
  gap: 6px;
}
.efield-label span { font-size: 14px; }

textarea.note {
  width: 100%;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 14px;
  color: var(--ink);
  border: 1.5px solid var(--border);
  border-radius: 10px;
  padding: 12px 14px;
  background: var(--gray);
  outline: none;
  resize: none;
  min-height: 80px;
  line-height: 1.6;
  transition: border-color .2s, background .2s;
}
textarea.note:focus { border-color: var(--red); background: var(--white); }
textarea.note::placeholder { color: #C8C6C2; font-style: italic; }
textarea.note.tall { min-height: 120px; }

/* Photo strip */
.photo-strip {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}
.photo-thumb {
  width: 80px; height: 80px;
  border-radius: 10px;
  object-fit: cover;
  border: 1px solid var(--border);
  position: relative;
  display: block;
}
.photo-thumb-wrap {
  position: relative;
  width: 80px; height: 80px;
  flex-shrink: 0;
}
.photo-thumb-wrap img {
  width: 100%; height: 100%;
  border-radius: 10px;
  object-fit: cover;
  border: 1px solid var(--border);
  display: block;
}
.photo-thumb-del {
  position: absolute;
  top: -5px; right: -5px;
  width: 20px; height: 20px;
  border-radius: 50%;
  background: rgba(0,0,0,0.55);
  color: #fff;
  border: none;
  cursor: pointer;
  font-size: 12px;
  display: flex; align-items: center; justify-content: center;
  z-index: 2;
}
.add-photo-btn {
  width: 80px; height: 80px;
  border-radius: 10px;
  border: 2px dashed var(--border);
  background: var(--gray);
  cursor: pointer;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2px;
  transition: all .15s;
  flex-shrink: 0;
  position: relative;
}
.add-photo-btn:hover { border-color: var(--red); background: var(--red-light); }
.add-photo-btn input[type=file] { position: absolute; inset: 0; opacity: 0; cursor: pointer; }
.add-photo-btn-icon { font-size: 20px; color: var(--muted); }
.add-photo-btn-label { font-size: 9px; font-weight: 600; color: var(--muted); text-transform: uppercase; }

/* Consumer blocks */
.consumer-list { display: flex; flex-direction: column; gap: 8px; }
.consumer-block {
  border: 1.5px solid var(--border);
  border-radius: 10px;
  overflow: hidden;
}
.consumer-block-header {
  background: var(--gray);
  padding: 9px 12px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 1px solid var(--border);
}
.consumer-block-title { font-size: 12px; font-weight: 700; color: var(--sub); }
.consumer-block-body { padding: 12px; display: flex; flex-direction: column; gap: 10px; }
.consumer-inline { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; }
.consumer-inline input {
  width: 100%;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 13px;
  color: var(--ink);
  border: 1.5px solid var(--border);
  border-radius: 8px;
  padding: 9px 12px;
  background: var(--gray);
  outline: none;
  transition: border-color .2s;
}
.consumer-inline input:focus { border-color: var(--red); background: var(--white); }
.consumer-inline input::placeholder { color: #C8C6C2; font-style: italic; }
.consumer-block textarea {
  width: 100%;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 13px;
  color: var(--ink);
  border: 1.5px solid var(--border);
  border-radius: 8px;
  padding: 9px 12px;
  background: var(--gray);
  outline: none;
  resize: none;
  min-height: 64px;
  transition: border-color .2s;
  line-height: 1.5;
}
.consumer-block textarea:focus { border-color: var(--red); background: var(--white); }
.consumer-block textarea::placeholder { color: #C8C6C2; font-style: italic; }
.mini-lbl { font-size: 11px; font-weight: 600; color: var(--muted); text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px; display: block; }

.add-small-btn {
  display: flex;
  align-items: center;
  gap: 6px;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 12.5px;
  font-weight: 600;
  color: var(--muted);
  background: none;
  border: 1.5px dashed var(--border);
  border-radius: 8px;
  padding: 9px 12px;
  cursor: pointer;
  width: 100%;
  transition: all .15s;
}
.add-small-btn:hover { border-color: var(--red); color: var(--red); }

.mini-lbl { font-size: 11px; font-weight: 600; color: var(--muted); text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px; display: block; }
.tiket-block { border: 1.5px solid var(--border); border-radius: 10px; overflow: hidden; margin-bottom: 8px; }
.tiket-header { background: var(--gray); padding: 9px 12px; display: flex; align-items: center; justify-content: space-between; border-bottom: 1px solid var(--border); }

/*  DELIVERABLES TAB  */
.deliv-wrap {
  padding: 16px;
  max-width: 100%;
  margin: 0 auto;
}

@media print {
  body { background: white; }
  .topbar, .tabnav, .fab, .fab-label { display: none !important; }
  .view { display: none !important; }
  #view-deliverables { display: block !important; }
  @page { size: A4 landscape; margin: 12mm; }
  .deliv-wrap { padding: 0; }
  .compare-scroll, .journey-scroll { overflow: visible; }
  .compare-tbl, .journey-tbl { min-width: unset; font-size: 10px; }
  .compare-tbl th, .compare-tbl td,
  .journey-tbl th, .journey-tbl td { padding: 6px 8px; }
  .compare-tbl td textarea, .journey-tbl td textarea { min-height: 36px; font-size: 10px; }
}

.deliv-card {
  background: var(--white);
  border: 1px solid var(--border);
  border-radius: 14px;
  margin-bottom: 16px;
  overflow: hidden;
}
.deliv-card-header {
  padding: 14px 18px;
  display: flex;
  align-items: center;
  gap: 10px;
  border-bottom: 1px solid var(--border);
  background: var(--gray);
}
.deliv-badge {
  background: var(--red);
  color: #fff;
  font-size: 10px;
  font-weight: 800;
  padding: 3px 8px;
  border-radius: 6px;
  letter-spacing: 0.5px;
}
.deliv-title { font-size: 14px; font-weight: 700; color: var(--ink); }
.deliv-sub { font-size: 12px; color: var(--muted); margin-top: 2px; }
.deliv-body { padding: 14px; }

/* Tabel perbandingan */
.compare-scroll { overflow-x: auto; -webkit-overflow-scrolling: touch; }
.compare-tbl { min-width: 780px; width: 100%; border-collapse: collapse; font-size: 12.5px; }
.compare-tbl th {
  background: var(--red);
  color: #fff;
  font-weight: 700;
  font-size: 11px;
  padding: 9px 11px;
  text-align: left;
  white-space: nowrap;
}
.compare-tbl th:first-child { background: var(--red-dark); min-width: 110px; }
.compare-tbl td { border-bottom: 1px solid var(--border); padding: 0; vertical-align: top; }
.compare-tbl tr:last-child td { border-bottom: none; }
.compare-tbl tr:nth-child(even) td { background: var(--gray); }
.compare-tbl td.aspect-cell {
  padding: 8px 11px;
  background: #F0EFED;
  border-right: 2px solid var(--border);
  vertical-align: middle;
}
.compare-tbl tr:nth-child(even) td.aspect-cell { background: #E8E7E4; }
.compare-tbl td textarea {
  border: none;
  background: transparent;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 12.5px;
  color: var(--ink);
  padding: 8px 11px;
  resize: none;
  outline: none;
  min-height: 52px;
  width: 100%;
  line-height: 1.5;
}
.compare-tbl td textarea::placeholder { color: #CCC; font-style: italic; }
.compare-tbl td.aspect-cell textarea {
  font-weight: 600;
  color: var(--sub);
  padding: 8px 11px;
}
.tbl-add-row {
  display: flex;
  align-items: center;
  gap: 6px;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 12px;
  font-weight: 600;
  color: var(--muted);
  background: none;
  border: none;
  padding: 10px 11px;
  cursor: pointer;
  transition: color .15s;
  border-top: 1px dashed var(--border);
  width: 100%;
  text-align: left;
}
.tbl-add-row:hover { color: var(--red); }

/* Journey map */
.journey-scroll { overflow-x: auto; -webkit-overflow-scrolling: touch; }
.journey-tbl { min-width: 860px; width: 100%; border-collapse: collapse; font-size: 12px; }
.journey-tbl th {
  background: var(--red);
  color: #fff;
  font-size: 10.5px;
  font-weight: 700;
  padding: 8px 9px;
  text-align: center;
}
.journey-tbl th:first-child { background: var(--red-dark); text-align: left; min-width: 90px; }
.journey-tbl th[contenteditable]:focus { outline: 2px solid rgba(255,255,255,0.5); }
.journey-tbl td { border: 1px solid var(--border); padding: 0; vertical-align: top; }
.journey-tbl td:first-child {
  background: #F0EFED;
  font-weight: 600;
  font-size: 11.5px;
  color: var(--sub);
  padding: 8px 9px;
  border-right: 2px solid var(--border);
  vertical-align: middle;
}
.journey-tbl td textarea {
  border: none;
  background: transparent;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 12px;
  color: var(--ink);
  padding: 7px 9px;
  resize: none;
  outline: none;
  min-height: 52px;
  width: 100%;
  line-height: 1.5;
}
.journey-tbl td textarea::placeholder { color: #CCC; font-style: italic; font-size: 11px; }
.journey-tbl tr.pain-row td { background: #FFF5F5; }
.journey-tbl tr.pain-row td:first-child { background: #F0E0E0; color: var(--red-dark); }
.journey-tbl tr.pain-row td textarea { color: #8B0000; }
.stage-add-btn {
  display: flex;
  align-items: center;
  gap: 6px;
  font-family: 'Plus Jakarta Sans', sans-serif;
  font-size: 12px;
  font-weight: 600;
  color: var(--muted);
  background: none;
  border: none;
  padding: 9px 11px;
  cursor: pointer;
  transition: color .15s;
  border-top: 1px dashed var(--border);
  width: 100%;
  text-align: left;
}
.stage-add-btn:hover { color: var(--red); }

/*  FAB (add entry)  */
.fab {
  position: fixed;
  bottom: 20px;
  right: 20px;
  width: 52px; height: 52px;
  border-radius: 50%;
  background: var(--red);
  color: #fff;
  border: none;
  font-size: 24px;
  cursor: pointer;
  box-shadow: 0 4px 16px rgba(192,57,43,0.35);
  display: flex; align-items: center; justify-content: center;
  transition: background .15s, transform .1s;
  z-index: 200;
}
.fab:hover { background: var(--red-dark); transform: scale(1.05); }
.fab:active { transform: scale(0.97); }
.fab-label {
  position: fixed;
  bottom: 26px;
  right: 82px;
  background: var(--ink);
  color: #fff;
  font-size: 12px;
  font-weight: 600;
  padding: 6px 12px;
  border-radius: 8px;
  pointer-events: none;
  opacity: 0;
  transition: opacity .2s;
  white-space: nowrap;
}
.fab:hover ~ .fab-label { opacity: 1; }

/* Empty state */
.empty-state {
  text-align: center;
  padding: 48px 20px;
  color: var(--muted);
}
.empty-state-icon { font-size: 48px; margin-bottom: 12px; display: block; }
.empty-state-title { font-size: 15px; font-weight: 700; color: var(--sub); margin-bottom: 6px; }
.empty-state-sub { font-size: 13px; line-height: 1.6; }
</style>
</head>
<body>

<!-- TOP BAR -->
<div class="topbar">
  <div class="topbar-title">Log Book After Sales</div>
  <div class="topbar-right">
    <button class="topbar-btn" onclick="switchTab('deliverables')"> Rangkuman</button>
    <button class="topbar-btn" onclick="exportCSV()">Ekspor CSV</button>
    <button class="topbar-btn primary" onclick="window.print()">Simpan PDF</button>
  </div>
</div>

<!-- TAB NAV -->
<div class="tabnav">
  <div class="tab active" onclick="switchTab('cover')" id="tab-cover">
    <span class="tab-icon"></span>
    <span class="tab-label">Identitas</span>
  </div>
  <div class="tab" onclick="switchTab('entries')" id="tab-entries">
    <span class="tab-icon"></span>
    <span class="tab-label">Entri</span>
  </div>
  <div class="tab" onclick="switchTab('deliverables')" id="tab-deliverables">
    <span class="tab-icon"></span>
    <span class="tab-label">Rangkuman</span>
  </div>
</div>

<!-- 
     VIEW: COVER / IDENTITAS
 -->
<div class="view active" id="view-cover">
  <div class="cover">
    <div class="cover-card">
      <div class="cover-top">
        <div class="cover-badge">Commercial Trainee Program · M1 · Consumer Insight</div>
        <div class="cover-title">Log Book<br>After Sales</div>
        <div class="cover-sub">Catatan lapangan peserta — toko & service center</div>
      </div>
      <div class="cover-fields">
        <div class="cover-field">
          <label>Nama Peserta</label>
          <input type="text" placeholder="Nama lengkap">
        </div>
        <div class="cover-field cover-field-row">
          <div>
            <label>Fasilitator</label>
            <input type="text" placeholder="Nama fasilitator">
          </div>
          <div>
            <label>Periode</label>
            <input type="text" placeholder="cth: Mei 2026">
          </div>
        </div>
        <div class="cover-field">
          <label>Wilayah / Area</label>
          <input type="text" placeholder="cth: Semarang, Jawa Tengah">
        </div>
      </div>
    </div>

    <div class="cover-tip">
       <strong>Petunjuk Penggunaan:</strong> Setiap kali peserta turun lapangan, tambah entri baru di tab <strong>Entri</strong>. Catat secara apa adanya — log book ini adalah catatan peserta, bukan laporan formal yang baku. Pada akhir periode, rangkum seluruh temuan di tab <strong>Rangkuman</strong>.
    </div>
  </div>
</div>

<!-- 
     VIEW: ENTRIES
 -->
<div class="view" id="view-entries">
  <div class="entries-wrap" id="entries-container">
    <!-- empty state -->
    <div class="empty-state" id="empty-state">
      <span class="empty-state-icon"></span>
      <div class="empty-state-title">Belum ada entri kunjungan</div>
      <div class="empty-state-sub">Klik tombol <strong>+</strong> di pojok kanan bawah<br>setiap kali melakukan kunjungan lapangan.</div>
    </div>
  </div>
</div>

<!-- 
     VIEW: RANGKUMAN TEMUAN
 -->
<div class="view" id="view-deliverables">
  <div class="deliv-wrap">

    <div style="padding: 4px 0 16px;">
      <div style="font-size:13px; color:var(--sub); line-height:1.7; background:var(--white); border:1px solid var(--border); border-radius:12px; padding:14px 16px; margin-bottom:10px;">
         <strong>Ini bukan laporan final.</strong> Isi bagian ini dari kumpulan entri lapangan yang sudah peserta catat. Tujuannya merangkum temuan awal sebagai bahan analisis dan presentasi nanti.
      </div>
      <div style="display:flex; gap:8px; margin-top:10px;">
        <button onclick="window.print()" style="flex:1; padding:12px; border:none; border-radius:10px; background:var(--red); color:#fff; font-family:'Plus Jakarta Sans',sans-serif; font-size:13px; font-weight:700; cursor:pointer;">
          Simpan PDF Landscape
        </button>
        <button onclick="exportCSV()" style="flex:1; padding:12px; border:1.5px solid var(--red); border-radius:10px; background:var(--white); color:var(--red); font-family:'Plus Jakarta Sans',sans-serif; font-size:13px; font-weight:700; cursor:pointer;">
          Ekspor ke CSV / Excel
        </button>
      </div>
    </div>

    <!-- D1: TABEL PERBANDINGAN -->
    <div class="deliv-card">
      <div class="deliv-card-header">
        <div>
          <div style="display:flex; align-items:center; gap:8px; margin-bottom:3px;">
            <span class="deliv-badge">D1</span>
            <span class="deliv-title">Tabel Perbandingan Layanan</span>
          </div>
          <div class="deliv-sub">Aspek bebas — isi dari temuan lapangan. Hasil ini bisa jadi pareto masalah terbesar.</div>
        </div>
      </div>
      <div class="deliv-body">
        <div class="compare-scroll">
          <table class="compare-tbl">
            <thead>
              <tr>
                <th>Aspek yang Diamati</th>
                <th>Standar / Ekspektasi</th>
                <th>Realita / Temuan Lapangan</th>
                <th>Bukti / Data Pendukung</th>
                <th>Gap / Pain Point</th>
              </tr>
            </thead>
            <tbody id="compare-body">
              <tr>
                <td class="aspect-cell"><textarea placeholder="cth: parkir, signage, alur registrasi, komunikasi status..."></textarea></td>
                <td><textarea placeholder="ekspektasi wajar konsumen..."></textarea></td>
                <td><textarea placeholder="apa yang peserta temukan di lapangan..."></textarea></td>
                <td><textarea placeholder="cth: Entri 1, Foto 2, Responden #1"></textarea></td>
                <td><textarea placeholder="gap atau pain point-nya..."></textarea></td>
                
              </tr>
              <tr>
                <td class="aspect-cell"><textarea placeholder="aspek lain..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="cth: Entri 2, Foto 1, Responden #3"></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                </tr>
              <tr>
                <td class="aspect-cell"><textarea placeholder="aspek lain..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="cth: Entri 3, Quote konsumen #2"></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
              </tr>
            </tbody>
          </table>
          <button class="tbl-add-row" onclick="addCompareRow()"> Tambah aspek</button>
        </div>
      </div>
    </div>

    <!-- D2: CATATAN ALUR PENGALAMAN KONSUMEN -->
    <div class="deliv-card">
      <div class="deliv-card-header">
        <div>
          <div style="display:flex; align-items:center; gap:8px; margin-bottom:3px;">
            <span class="deliv-badge">D2</span>
            <span class="deliv-title">Catatan Alur Pengalaman Konsumen</span>
          </div>
          <div class="deliv-sub">Tahap bebas — isi sesuai yang peserta temui. Bisa: parkir, tanya staf, antrian, jelaskan keluhan, tunggu, ambil produk, dll.</div>
        </div>
      </div>
      <div class="deliv-body">
        <div class="journey-scroll">
          <table class="journey-tbl" id="journey-tbl">
            <thead>
              <tr>
                <th>Tahap yang Dialami Konsumen</th>
                <th>Cerita / Aktivitas Konsumen</th>
                <th>Channel / Lokasi</th>
                <th>Hambatan / Pain Point</th>
                <th>Emosi / Reaksi Konsumen</th>
                <th>Bukti / Quote / Foto</th>
                
              </tr>
            </thead>
            <tbody id="journey-body">
              <tr>
                <td class="aspect-cell"><textarea placeholder="cth: cari lokasi SC"></textarea></td>
                <td><textarea placeholder="apa yang dilakukan & dialami..."></textarea></td>
                <td><textarea placeholder="cth: Google Maps, telepon..."></textarea></td>
                <td><textarea placeholder="hambatan yang ditemui..."></textarea></td>
                <td><textarea placeholder="cth: bingung, frustrasi..."></textarea></td>
                <td><textarea placeholder="quote langsung / foto..."></textarea></td>
                </tr>
              <tr>
                <td class="aspect-cell"><textarea placeholder="cth: datang ke toko"></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
              </tr>
              <tr>
                <td class="aspect-cell"><textarea placeholder="cth: parkir"></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
              </tr>
              <tr>
                <td class="aspect-cell"><textarea placeholder="cth: tanya staf"></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
              </tr>
              <tr>
                <td class="aspect-cell"><textarea placeholder="cth: antrian"></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
                <td><textarea placeholder="isi..."></textarea></td>
              </tr>
            </tbody>
          </table>
          <button class="tbl-add-row" onclick="addJourneyRow()"> Tambah tahap</button>
        </div>

        <div style="margin-top:14px;">
          <div style="font-size:11px; font-weight:700; color:var(--muted); text-transform:uppercase; letter-spacing:0.7px; margin-bottom:6px;">Pain point paling kritis dari seluruh alur</div>
          <textarea class="note" placeholder="Dari semua tahap, mana yang paling berdampak ke konsumen? Jelaskan singkat..."></textarea>
        </div>
      </div>
    </div>

  </div>
</div>

<!-- FAB -->
<button class="fab" onclick="addEntry()" title="Tambah Entri Baru"></button>
<div class="fab-label">Entri Baru</div>

<script>
let entryCount = 0;

//  TABS 
function switchTab(name) {
  document.querySelectorAll('.view').forEach(v => v.classList.remove('active'));
  document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
  document.getElementById('view-' + name).classList.add('active');
  document.getElementById('tab-' + name).classList.add('active');

  // show/hide FAB
  document.querySelector('.fab').style.display = name === 'entries' ? 'flex' : 'none';
}

//  ENTRIES 
function addEntry() {
  entryCount++;
  const empty = document.getElementById('empty-state');
  if (empty) empty.remove();

  const container = document.getElementById('entries-container');
  const card = document.createElement('div');
  card.className = 'entry-card';
  card.id = 'entry-' + entryCount;

  const today = new Date().toISOString().split('T')[0];

  card.innerHTML = `
    <div class="entry-header" onclick="toggleEntry(${entryCount})">
      <div class="entry-header-left">
        <div class="entry-date-loc">
          <span class="entry-date" id="elabel-date-${entryCount}">${today}</span>
          <span class="entry-loc" id="elabel-loc-${entryCount}">Lokasi belum diisi</span>
        </div>
        <div class="entry-preview" id="elabel-preview-${entryCount}">Klik untuk membuka catatan</div>
      </div>
      <div class="entry-actions" onclick="event.stopPropagation()">
        <button class="entry-btn del" onclick="deleteEntry(${entryCount})" title="Hapus entri"></button>
      </div>
    </div>
    <div class="entry-body open" id="ebody-${entryCount}">

      <!-- INFO -->
      <div class="efield">
        <div class="efield-label"><span></span> Info Kunjungan</div>
        <div style="display:grid; grid-template-columns:1fr 1fr; gap:8px; margin-bottom:8px;">
          <input type="date" value="${today}" style="font-family:'Plus Jakarta Sans',sans-serif;font-size:13px;color:var(--ink);border:1.5px solid var(--border);border-radius:8px;padding:9px 12px;background:var(--gray);outline:none;transition:border-color .2s;" oninput="updateEntryLabel(${entryCount})" onchange="updateEntryLabel(${entryCount})">
          <input type="time" style="font-family:'Plus Jakarta Sans',sans-serif;font-size:13px;color:var(--ink);border:1.5px solid var(--border);border-radius:8px;padding:9px 12px;background:var(--gray);outline:none;" placeholder="Jam">
        </div>
        <input type="text" id="eloc-${entryCount}" style="width:100%;font-family:'Plus Jakarta Sans',sans-serif;font-size:13px;color:var(--ink);border:1.5px solid var(--border);border-radius:8px;padding:9px 12px;background:var(--gray);outline:none;margin-bottom:8px;" placeholder="Nama & alamat lokasi" oninput="updateEntryLabel(${entryCount})">
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;">
          <select style="font-family:'Plus Jakarta Sans',sans-serif;font-size:13px;color:var(--ink);border:1.5px solid var(--border);border-radius:8px;padding:9px 12px;background:var(--gray);outline:none;appearance:none;">
            <option value="">Tipe lokasi</option>
            <option> Toko / Dealer resmi</option>
            <option> Service Center</option>
            <option> Toko multi-brand</option>
            <option> Home service</option>
          </select>
          <input type="text" style="font-family:'Plus Jakarta Sans',sans-serif;font-size:13px;color:var(--ink);border:1.5px solid var(--border);border-radius:8px;padding:9px 12px;background:var(--gray);outline:none;" placeholder="Durasi kunjungan">
        </div>
      </div>

      <!-- OBSERVASI (dinamis) -->
      <div class="efield">
        <div class="efield-label">Observasi Lapangan</div>
        <div id="obs-list-${entryCount}"></div>
        <button class="add-small-btn" onclick="addObservasi(${entryCount})">+ Tambah Observasi</button>
      </div>

      <!-- KONSUMEN -->
      <div class="efield">
        <div class="efield-label"><span></span> Wawancara Konsumen</div>
        <div class="consumer-list" id="consumers-${entryCount}"></div>
        <button class="add-small-btn" onclick="addConsumer(${entryCount})"> Tambah Responden</button>
      </div>

      <!-- TEMUAN HARI INI -->
      <div class="efield">
        <div class="efield-label"><span></span> Temuan & Insight Hari Ini</div>
        <textarea class="note" placeholder="Apa hal paling penting yang peserta pelajari dari kunjungan ini? Masalah yang paling mencolok, pola yang mulai terlihat, atau hal yang perlu ditindaklanjuti..."></textarea>
      </div>

    </div>`;

  container.appendChild(card);
  card.scrollIntoView({ behavior: 'smooth', block: 'start' });
}

function toggleEntry(id) {
  const body = document.getElementById('ebody-' + id);
  body.classList.toggle('open');
}

function deleteEntry(id) {
  if (confirm('Konfirmasi: hapus entri ini?')) {
    document.getElementById('entry-' + id).remove();
    if (!document.querySelector('.entry-card')) {
      const container = document.getElementById('entries-container');
      container.innerHTML = `<div class="empty-state" id="empty-state">
        <span class="empty-state-icon"></span>
        <div class="empty-state-title">Belum ada entri kunjungan</div>
        <div class="empty-state-sub">Klik tombol <strong>+</strong> di pojok kanan bawah<br>setiap kali melakukan kunjungan lapangan.</div>
      </div>`;
    }
  }
}

function updateEntryLabel(id) {
  const loc = document.getElementById('eloc-' + id)?.value;
  const date = document.querySelector(`#entry-${id} input[type=date]`)?.value;
  if (loc)  document.getElementById('elabel-loc-' + id).textContent = loc;
  if (date) document.getElementById('elabel-date-' + id).textContent = date;
  const firstObs = document.querySelector(`#obs-list-${id} textarea.note`);
  if (firstObs?.value) document.getElementById('elabel-preview-' + id).textContent =
    firstObs.value.substring(0, 60) + (firstObs.value.length > 60 ? '...' : '');
}

// OBSERVASI DINAMIS
let obsCounts = {};
function addObservasi(entryId) {
  if (!obsCounts[entryId]) obsCounts[entryId] = 0;
  obsCounts[entryId]++;
  const n = obsCounts[entryId];
  const list = document.getElementById('obs-list-' + entryId);
  const block = document.createElement('div');
  block.className = 'tiket-block';
  block.style.marginBottom = '10px';
  block.innerHTML = `
    <div class="tiket-header" style="background:var(--gray); padding:9px 12px; display:flex; align-items:center; justify-content:space-between; border-bottom:1px solid var(--border);">
      <span style="font-size:12px; font-weight:700; color:var(--sub);">Observasi #${n}</span>
      <button class="entry-btn del" style="width:28px;height:28px;" onclick="this.closest('.tiket-block').remove()">x</button>
    </div>
    <div style="padding:12px; display:flex; flex-direction:column; gap:10px;">
      <div>
        <span class="mini-lbl">Catatan Observasi #${n}</span>
        <textarea class="note" placeholder="Catat kondisi yang diamati — kondisi fisik, parkir, signage, display produk, staf, antrian, suasana, dll. Catat secara apa adanya..."></textarea>
      </div>
      <div>
        <span class="mini-lbl">Foto Observasi #${n}</span>
        <div class="photo-strip" id="photos-obs-${entryId}-${n}">
          <div class="add-photo-btn">
            <input type="file" accept="image/*" capture="environment" multiple onchange="handlePhotos(this, 'photos-obs-${entryId}-${n}')">
            <div class="add-photo-btn-icon"></div>
            <div class="add-photo-btn-label">Tambah Foto</div>
          </div>
        </div>
      </div>
    </div>`;
  list.appendChild(block);
}

//  CONSUMER 
let consumerCounts = {};
function addConsumer(entryId) {
  if (!consumerCounts[entryId]) consumerCounts[entryId] = 0;
  consumerCounts[entryId]++;
  const n = consumerCounts[entryId];
  const list = document.getElementById('consumers-' + entryId);
  const block = document.createElement('div');
  block.className = 'consumer-block';
  block.innerHTML = `
    <div class="consumer-block-header">
      <span class="consumer-block-title">Responden #${n}</span>
      <button class="entry-btn del" style="width:28px;height:28px;font-size:12px;" onclick="this.closest('.consumer-block').remove()">×</button>
    </div>
    <div class="consumer-block-body">
      <div class="consumer-inline">
        <input type="text" placeholder="Usia / kisaran">
        <input type="text" placeholder="Produk / masalah">
      </div>
      <div>
        <span class="mini-lbl">Cerita konsumen</span>
        <textarea placeholder="Catat pernyataan atau cerita konsumen secara apa adanya..."></textarea>
      </div>
      <div>
        <span class="mini-lbl">Pain point / keluhan</span>
        <textarea placeholder="Hambatan atau keluhan utama yang disampaikan konsumen"></textarea>
      </div>
      <div>
        <span class="mini-lbl">Harapan</span>
        <textarea placeholder="Harapan atau perbaikan yang diinginkan konsumen"></textarea>
      </div>
    </div>`;
  list.appendChild(block);
}

//  PHOTOS 
function handlePhotos(input, stripId) {
  const strip = document.getElementById(stripId);
  const addBtn = strip.querySelector('.add-photo-btn');
  Array.from(input.files).forEach(file => {
    const reader = new FileReader();
    reader.onload = e => {
      const wrap = document.createElement('div');
      wrap.className = 'photo-thumb-wrap';
      wrap.innerHTML = `<img src="${e.target.result}" alt="foto"><button class="photo-thumb-del" onclick="this.closest('.photo-thumb-wrap').remove()">×</button>`;
      strip.insertBefore(wrap, addBtn);
    };
    reader.readAsDataURL(file);
  });
  input.value = '';
}

//  RANGKUMAN TEMUAN 
function addCompareRow() {
  const tbody = document.getElementById('compare-body');
  const tr = document.createElement('tr');
  tr.innerHTML = `
    <td class="aspect-cell"><textarea placeholder="aspek baru..."></textarea></td>
    <td><textarea placeholder="standar / ekspektasi..."></textarea></td>
    <td><textarea placeholder="realita di lapangan..."></textarea></td>
    <td><textarea placeholder="cth: Entri 1, Foto 2, Responden #1"></textarea></td>
    <td><textarea placeholder="gap / pain point..."></textarea></td>
    `;
  tbody.appendChild(tr);
}

function addJourneyRow() {
  const tbody = document.getElementById('journey-body');
  const tr = document.createElement('tr');
  tr.innerHTML = `
    <td class="aspect-cell"><textarea placeholder="tahap baru..."></textarea></td>
    <td><textarea placeholder="cerita / aktivitas..."></textarea></td>
    <td><textarea placeholder="channel / lokasi..."></textarea></td>
    <td><textarea placeholder="hambatan / pain point..."></textarea></td>
    <td><textarea placeholder="emosi / reaksi..."></textarea></td>
    <td><textarea placeholder="cth: Entri 2, Quote: '...'"></textarea></td>
    `;
  tbody.appendChild(tr);
}

// ── EKSPOR CSV ──
function exportCSV() {
  let csv = '';

  // Sheet 1: Tabel Perbandingan
  csv += 'TABEL PERBANDINGAN LAYANAN\n';
  csv += '"Aspek yang Diamati","Standar / Ekspektasi","Realita / Temuan Lapangan","Bukti / Data Pendukung","Gap / Pain Point"\n';
  document.querySelectorAll('#compare-body tr').forEach(row => {
    const cells = row.querySelectorAll('textarea');
    const vals = Array.from(cells).map(t => '"' + (t.value || '').replace(/"/g, '""') + '"');
    if (vals.length) csv += vals.join(',') + '\n';
  });

  csv += '\n';

  // Sheet 2: Catatan Alur Konsumen
  csv += 'CATATAN ALUR PENGALAMAN KONSUMEN\n';
  csv += '"Tahap","Cerita / Aktivitas","Channel / Lokasi","Hambatan / Pain Point","Emosi / Reaksi","Bukti / Quote"\n';
  document.querySelectorAll('#journey-body tr').forEach(row => {
    const cells = row.querySelectorAll('td');
    const vals = Array.from(cells).map(td => {
      const ta = td.querySelector('textarea');
      return '"' + (ta ? ta.value : td.textContent || '').replace(/"/g, '""') + '"';
    });
    if (vals.length) csv += vals.join(',') + '\n';
  });

  csv += '\n';

  // Sheet 3: Rekap Entri Lapangan
  csv += 'REKAP ENTRI LAPANGAN\n';
  csv += '"No. Entri","Tanggal","Lokasi","Catatan Observasi","Temuan Hari Ini"\n';
  document.querySelectorAll('.entry-card').forEach((card, i) => {
    const date = card.querySelector('input[type=date]')?.value || '';
    const loc  = card.querySelector('input[type=text]')?.value || '';
    const obs  = card.querySelector('textarea.note.tall')?.value || '';
    const notes = card.querySelectorAll('textarea.note');
    const temuan = notes[notes.length - 1]?.value || '';
    csv += `"${i+1}","${date}","${loc.replace(/"/g,'""')}","${obs.replace(/"/g,'""')}","${temuan.replace(/"/g,'""')}"\n`;
  });

  // Download
  const blob = new Blob(['\uFEFF' + csv], { type: 'text/csv;charset=utf-8;' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'LogBook_AfterSales_M1_Ekspor.csv';
  a.click();
  URL.revokeObjectURL(url);
}

// Init
switchTab('cover');
document.querySelector('.fab').style.display = 'none';
</script>
</body>
</html>
